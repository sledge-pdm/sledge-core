# Project Types

- プロジェクトで使用する型をここに置く
- 基本的にアプリからプロジェクトの内容物の型を参照するときはこれらを利用する(frascoなどの型は除く)

- **これらの型は基本的に最新のアプリにおけるランタイム内容で扱う型であり、同時に最新Project(Vn)が保持する型でもある**
  これらを一元的にproject_typesで管理することで保存/読み出しの際の取り扱いを堅牢なものにする。